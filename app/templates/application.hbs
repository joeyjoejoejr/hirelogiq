<header>
  <ul>
    <li>{{#linkTo 'candidates'}}HireLogIQ{{/linkTo}}</li>
    {{#if isLoggedIn}}
      <li><a href="#" {{action logout}}>Logout</a></li>
    {{else}}
      <li>{{#linkTo 'login'}}Login{{/linkTo}}</li>
    {{/if}}
  </ul>
</header>

<div class="container" id="main">
  {{outlet}}
</div>
